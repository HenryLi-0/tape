# 12/24/2024 - 61st Update!

so here we go! fourth day of winter break! (i dont remember what happened on saturday, but sunday i was working on an essay and yesterday, my group and i just speedran the rest of an entire project in one day) so im gonna ingore an assignment for a while (should be pretty simple), and lets get started!

so, the other day i had another idea to simplify the math stuff, and it was to instead use an `Calculation` node or something, where you can input something like `sqrt(x)` or `(x+1)**2` and it interprets it python style! of course, it would be a bad idea to just use `eval()` as this is clearly one of the clearest unsafe moments to use it (because, you know, its literally evaluating whatever you put in it. like a print statement for example.). so, i guess im gonna write that later!

additionally, another idea was have music... which is a bit ambitious, but we have 8 days left of break, its very possible to do this! so, with the `File Location` 

so heres the programming update roadmap im planning on:

- Tape v2:
  - Full rewrite of Tape v1
  - Most nodes and stuff (not including music)
  - Simple music nodes
  - All tabs
- Tape v2.1:
  - Certaintly bug fixes
  - Advanced tooltips! (basically, hover over to see what to do)
  - More advanced music nodes
  - Likely performance updates
  - More customization
  - Plugin/custom nodes support
  - Other feedback

and other stuff i probably forgot, but simply, tape v2 will be mostly just tape v2 and its basics to do very good stuff, and tape v2.1 would improve on it and add more customization updates

ok, so im gonna update the old manuals and stuff, and quickly commit, then we begin!

ok, just updated the manual structure, gonna copy the node stuff later (things should be close to finished/finalized but it might change, who knows), so im gonna start working on the code stuff now!

ok, so copying the plan over:

- DONE - Sledgehammer
  - Archive Tape v1

ok, tape v1 has been [archived](https://github.com/HenryLi-0/tape/releases/tag/archive-v1)! next, sledgehammer...

ok, `render.py` has been updated! this means that, in theory, everything is broken now as the rendering system has been switched!

ok, now ive updated lots of subsystems and settings, but we still need to update `interface.py` and the interface system in general. we also need to add the new fonts! (i know, we probably wont more than like 2, but still) additionally, `settings.py` is still a bit cluttered, and we should move icons to their own little folder

ok, its the end of the day, and IVO v2 is basically moved in! just need to clean up the icons and mvoe them into their own folder, along with celaning up `settings.py`, but we should almost be good for programming! (wow that was a lot of manual changes to each file...)

encountered a weird bug, but now when i investigate it closer, its just gone. so yeah, idk, i guess its just gone? but now, mostly everything from Tape v1 relating to how Tape v1 worked (graph based editor, timeline, etc.) has been mostly removed, and IVO has been updated to v2, so lets see where this takes us! first thing tommorow, updating `settings.py` and files, then, a couple checks that everything works, and then we begin!

ok, exciting stuff, good night!