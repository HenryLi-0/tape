# 12/28/2024 - 65th Update!

ok, so i did not wake up earlier today. actually i did, but fell back asleep for everyone of my four alarms, so thats great. anyways, now that its 2:40 PM, time to continue!

oh wow turns out im going to get a hair cut right now. yay productivity!

ok im back at 4:45 PM, and uh oh i just typed `Boolean(!inputNode.get())`... (the exclamation mark would work as `not()` in java, but not python in this case (as far as i know))

ok, so some logic stuff has been done, of course, not tested yet, but *should* work in theory! also, in the background, i have a video playing, and may entire screen is going crazy, so im gonna restart my computer...

ok, im back, (its 9 PM now oops), logic is pretty much good and ready for testing, time to backtrack and finish the path stuff!

turns out that path logic is a bit hard, and pretty confusing... additionally, gonna backtrack even more to the optimization ideas from yesterday, where we create an object on initalization that's going to be the result (and also always be the result object) but before then, quick commit!

just got an idea that i should probably write down, tape v2.1 should probably also have a graph based editor that people can use to convert graphs into path nodes, probably with some form of graph, then, "insert nodes" button or something!

ok, just realized something, we should format nodes like this:

```python
class ExampleNode(Node):
    def __init__(self, inputNode):
        pass # prepares variables and stuff and initalizes the output object
        self.set(inputNode)
    def set(self, inputNode):
        pass # sets the input variables (like the location of the objects)
    def get(self):
        pass # does the calculations
        return None

```

so we dont have to keep recreating nodes, we can just initiate them, pass in the objects in set, and get would return the calculations with the nodes that were set in set

of course, this also does mean i do have a bit of rewriting some bits to do...

ok this is getting a bit confusing because calculations are done in get() but ive been putting them in set()...

wow, so all of `node_primatives.py` has been refactored (untested), so im gonna head over to `test2.py` real quick, and then probably fix a lot of stuff...

ok, things are working (i just found out that `addError()` part of the `Node` class was broken the whole time!) with the same test that was run on 12/26/2024 line 19, it takes `1.795325183868408e-05`, which is somehow faster than before (which shouldn't happen?)? well, to be honest, we're dealing with really small time frames that in the grand scheme of things, isn't too too too significant, especially since its so small and an extra window could influence the results

ok, wrote a couple `TO-DO` messages, but one final thing before i say good night, this will be commit number 200! (no, thats an exclamation mark, not a factorial operation)

![199 commits](</updatelogs/images/122024/12282024 - 1.png>)

its at 199 commits right now, but it'll be at 200 after this one! so thats a pretty big milestone! anyways, we're falling behind on development (uh oh), so maybe ill wake up earlier tommorow (can't wait to see how hard it is getting back to the school schedule...)

anyways, that's it for me today, good night!