# 1/12/2025 - 70th Update!

ok, we need to rethink a couple things! so we have the current issue of that our logic breaks down when it comes to `get()` with multiple outputs, as what are you getting? what does `get()` with a coordinate look like?

so a couple ideas are:
- `get()` could return a tuple/dictionary, with all the infor needed in it
- `get("attribute")` could also work
- no `get()`, but instead intaking the object and grabbing bits out of it, and slapping it back together

so after consulting the omniscient AI, here's what we're going to do, and lets make this quick before im being forced to go to sleep early. instead of using `get()`, it'll be repurposed to `update()`, and instead do the calculations. instead of having a getter method, we first hide all the properties by name mangling by adding `__` infront of variable names, and then do `@property` decorators for grabbing the bits that are needed. 

also, every day i stare in front of FRC and Java, im missing some of their features, (i like public and private variables) anyways, REFACTORING, GO!

no way, did he refactor it again? its like the third time. why does this guy like refactoring so much?

honestly, i have no clue how this happened like three times. infact, the previous rounds of refactoring havent even been completed yet.

ok, all of `node_primatives.py` should be refactored now! hasn't been tested yet, but im currenlty being told to go to sleep, so good night!