# 1/19/2025 - 73rd Update!

time to code

ok, so been adding some of the `@Display` decorators, and i just thought that our current naming convention of "primatives (like the most primative of primatives), use `value`, but everything else uses `output`". for the sake of consistency, although that made sense, it might be a good idea to switch to `value`, so you dont have to go in circles looking for nodes to write `self.output.value`, and instead just do `self.value.value`

on the other hand, it doesnt really make much sense and would be hard to realize when you're at "the bottom", so let me just revert those changes...

ok `@Display` decorators have been added! ok, UI time! gonna quickly skecth up something like the UI... never mind, it'll be fast to just program it i guess! just realized that if something only has one display, but like 50 inputs and outputs, cramming it all or stretching it out might look a little silly, so i guess just say "you should have a similar amount of both? (makes sense, since you wont have like many atrributes that you can't display)"

steps to init node:
- create template box, based on displays, inputs, and outputs (template box should be reusable)
  - name
  - dividers
  - input and output names
  - display names and boxes

steps to render node:
- copy template box
- add details
- render on screen

ok, lets get going! one thing to consider, is that the template box may or may not be recreated every time zoom happens, since zooming in and out requires more or less pixels, depending on the scale, but we'll assume its at 100% (or 1) (or normal) scale for now!

ok, so some things have been done, but lots more to go, split `VisualNode()` into its own file, and fixed some weird circular import stuff, so time to do more UI!

ok i just got back from shovelling snow, which is fun when my legs hurt after running at maximum speed down the robotics hallway four times (ouch, but was pretty speedy/fun!)

does python have enums? well technically yes, but im just getting annoyed that having to write `displayText("text", "m")` has a `"m"`, because that `7` that was previously there was being registed in the if elif elif else statement as `FONT_LARGE`... maybe ivo v3...

oh hey, look at the time, its 11:55 PM here in NYC! im gonna be told to sleep, so i *might* see you on github mobile in a bit! (also, big issue, we're REALLY falling behind on schedule, so might need to grind the next few days... (well, there goes my studying for the midyears...))