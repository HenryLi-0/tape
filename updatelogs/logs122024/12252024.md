# 12/25/2024 - 62nd Update!

wow merry christmas, its that time of the year again! (oh and it also means theres only a week of winter break left, not including today... uh oh)

ok, things have been cleaned up and `settings.py` has been updated!

- DONE - Prepare for programming!
  - DONE - Add new IVO v2 system in (will likely be manual file to file changes, since Tape already has some pretty useful functions that are kinda Tape specific that were removed in the IVO repository)
  - DONE - Make sure everything is updated and ready for programming

ok, whats next on the to do list... concept art stuff? i guess ill work on it, actually its pretty much done anyways

- ig - Concept Art
  - ig - Complete concept art and colors for things like nodes, positions of stuff on the screen, etc.

you know what, lets work on the node system right now, well, at least the insides and the code, since the actual node system is essentially the same thing, just with a GUI and lines to symbolize the ins and outs

ok, so in real life, im working in the living room while my sibliings are watching a movie, so im getting very distracting, so lets work on the Node docs, anyways, the idea was bascially that the Node system beneath would be Objects that are going in and out, so lets start with that!

wait a second... `node.py`? like node.js??? (wow i should try learning webdev again)

ok so i was today years old when i learned that python doesnt really support overloading functions like java, so thats sad, but i guess i could also do something like

```
class Number:
    def __init__(thing = None):
        if thing == None:
            thing = 0
        print(thing)
```

something like that

ok, so its been like around 2 hours and the beginning bits (the nodes in the basic section) have been mostly done! the units havent been done yet because i have no clue what to do with it, but im taking a lot of inspiration from FRC and wpilib to do it

ok i cant really concentrate with the movie in the background, but ill try to persevere! anyways, heres a commit

```
from subsystems.node import *

node1 = Number(0)
node2 = Number(10)
node3 = Boolean(True)
node4 = Random(node1, node2, node3)
node4.get()
```

ok, its working now!

```
node1 = FileLocation(String("C:/Users/henrys/Desktop/Projects/Github Repositories/tape/nonexistence.png"))
print(node1.getError())
```

`FileLocation` is also working now (fixed some errors and just added file existence checking)!

YES! CONVERTING BETWEEN TIME UNITS ARE WORKING!

```
print(Milliseconds(Days(Seconds(Milliseconds(Days(Milliseconds(Number(10))))))).get().get())
```

ok, and now degrees and radians are converting! currently, not really sure what i can do about `Pixels`, as its just supposed to convey a point on the screen, but we'll see. actually, just thought about it, and i think it'll probably work as a wrapper for numbers that will work with Frame(Coordinate(Pixel(...), Pixel(...)), Time(...))

wait a second, i just got a really good idea (well, that kinda depends on who you ask), but here it is: so when you do Coordinate(Number, Number), it really means like the percent of your screen, so if Coordinate(0, 0) represents the top left, with coordinate (0,0), but Coordinate(1, 1) represents the bottom right, with coordinate (max, max), so it acts more as like a percent, while Coordinate(Pixel, Pixel) does it exactly

ok, so one more thing before ending today, i plan on making `node.py` easily movable, as in it doesn't require `settings.py` or any outside interaction at all. instead, i want to make it, taking a bit of inspiration from OnShape FeatureScripts, basically use itself inside the code, as in like `node.py`, renamed `node_primitives.py` is the true basic system, and `node_addon.py` or something is like based on top of it, and everything is imported through one file importing everything

basically, making it look like everything here is a sort of plugin like, so its a bit more intutive for others to create their own plugins and custom nodes

but first, while everything works, quick commit! and good night, hopefully i'll wake up earlier tommorow!