# 1/30/2025 - 79th Update!

ok, i just speedran all my homework today, and its only 5:08 PM, and im planning on coding until 2 AM, so thats like probably 8 hours, when taking into account dinner and random things, so lets go! one thing i miss about arcade was the way that the goal you set kept you accountable, so lets make a nice plan/roadmap for what we plan on doing!

-  5:00 PM -  6:00 PM: Finish refactoring nodes and finish all nodes code-side
-  6:00 PM -  7:00 PM: GUI node fill-in
-  7:00 PM -  8:00 PM: Estimated Dinner time, continue if not
-  8:00 PM -  9:00 PM: Node connections GUI, flesh everything out
-  9:00 PM - 10:00 PM: Animation Tab, Render Logic
- 10:00 PM - 11:00 PM: Animation Controls, Exporting
- 11:00 PM - 12:00 AM: Other Tabs
- 12:00 AM -  1:00 AM: Saving/Loading
-  1:00 AM -  2:00 AM: Bug fixing

ok, then, 5:14 PM now, time to start! (also, of course, this schedule isn't to limit anything to an hour, its just to stay on track! also, if i finish an hour's work before it ends, i'll start the next one!)

ok, `simplify()` in `Unit` nodes has now been made lighter! (returns raw value on input, ex. in degrees for angles, in pixels for distances, and in seconds for time, all in `int`/`float` form) ok, its only 5:42 PM and my brain is kinda already melting (path stuff is very weird to make sense of)

ok, lets talk this out! so, new idea: `Frames` (multiple) -> `Path` (stores it, cant do anything) -> `PathCalculator` (used to be `PathType`, now does what `PathAtTime` does except its combined with `PathType`) -> `Coordinate` or `Unit` (end result)

6:02 PM and havent finished nodes yet, but getting there! ok, its now 6:23 PM, and im just gonna push! its not ready yet, but just in case something breaks! also, now `validate()` checks the needed nodes for errors (if there is one, it doesnt allow the `update()` to run, basically breaking everything that's after it, which is probably intended behavior!)

ok, think refactoring is ok for now, just need to do the node logic stuff, so let me update that schedule a bit...

-  5:00 PM -  6:45 PM: Finish refactoring nodes and most nodes code-side
-  6:45 PM -  7:15 PM: GUI node fill-in
-  7:15 PM -  7:30 PM: Estimated Dinner time, continue if not
-  7:30 PM -  9:00 PM: Node connections GUI, flesh everything out (node path stuff)
-  9:00 PM - 10:00 PM: Animation Tab, Render Logic
- 10:00 PM - 10:45 PM: Animation Controls, Exporting Video
- 10:45 PM - 12:00 AM: Other Tabs
- 12:00 AM - 12:30 AM: Saving/Loading
- 12:30 AM -  2:00 AM: Bug fixing

ok, just modified the macropad to have a `git add .` and `git push` macro! idk, might speed things up? (probably not, but whatever!)

anyways, GUI time! (its 6:49 PM) ok, its now 7:08 PM and i have been debugging the weird connection bug, and it seems working! but now the triangles are weirdly positioned... (uh oh gotta push more things back) ok, now the triangle thing is fixed! (so i was multiplying and adding `r_outputYMul` instead of multiplying by `r_outputYMul` and adding `r_outputYAdd`... oops) ok, quick push!