# 12/26/2024 - 63rd Update!

ok, almost a full stack of updates! (wow we're almost at 2^6 updates!)

anyways, started things off by splitting `node.py` into parts, and some settings! also, yesterday i realized but forgot to write that i realized that there arent many images around here anymore, which ill try to get a bit more of before this month ends!

oh here's one from yesterday! the node system in code form is looking good! (yes its a bunch of classes in classes)

![wow node system!](</updatelogs/images/122024/12262024 - 1.png>)

wow, and i guess i could still do that enter up arrow key combination i do to add these pictures! anyways, lets get back to work on `node_tape.py`, now that we have a `settings.py` inside `subsystems/`!

a small problem i just realized is that the addition opperation would be a bit difficult to add as in for everything, as it is kinda one function for everything, so it might need its own thing after `node_tape.py`, so probably something like `node_operations.py`! anyways, quick commit...

ok, so i was just writing the `Coordinate` node, and i just suddenly felt that this code looks very java-like... (so many getters, setters, and classes...)

java flashbacks aside, i wanna see how fast the code can generate tons of nodes, ill be right back...

ok, so it appears that, with n=1000000 (that is, the amount of test times that were averaged out) is `2.7155339002609252e-05`, or `0.00002` seconds per node, or `50000` nodes per second (using `Number()`)

so, if we only calculate a number per frame with 30 FPS, thats `27.7` minutes of footage a second! obviously, this just sounds good, as in reality, it would be slower as you're not gonna be rendering an animation with a single `Number()` node.

however, this does mean that we can create nodes really fast, we just need to make processing efficient

ok so im back from rethinking how we're doing this (classes or functions?), and i think, after my conversation with the omniscient AI, that we should keep it with classes

additionally, i just realized that we should have many under-the-hood optimizations, such as inputting in a scaled version of the actual screen size, so that we only calculate pixels that actually get rendered on the screen, not render everything and scale it down (lost data that, at the end of the day, is kinda of a waste of processing power and time)

so, thats a future me problem, back to `node_tape.py` to work on `Path` nodes!

ok, im off to dinner real quick, so, im gonna think more about this stuff (then i have a meeting after 8, where ill be speedrunning CAD!)

turns out the meeting has been pushed to 8:30...

ok, so turns out that the meeting lasted way longer than expected, which i expected, so now its 12:30 AM. however, just to write what updates are in this commit, IO stuff in `node_primatives.py` had some work, along with the path system in `node_tape.py`! still working on how to make this all fit together nicely, but we should be getting there! 

im guessing paths would be some sort of method reference like object, that are inserted into the SPRITE node, which would provide a `SPRITE_DATA` node, or something under the hood, that gets fed into the `RENDER` node that is the thing that actually takes in the `SPIRTE_DATA` node, unwraps the functions and numbers and stuff, and does the image processing and stuff

so just a couple minutes ago, i finished hacking together a featurescript in onshape that creates a obama origin pyramid (functions like an origin cube)! it was a bit frustrating but fun, so here the result!:

![obamagin](</updatelogs/images/122024/12262024 - 2.png>)

with that said, good night! (hopefully i actually wake up earlier tommorow!)