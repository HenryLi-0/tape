# 10/31/2024 - 51st Update!

update 51! so, currently still working on upgrade ivo, decided to take a look into updating how we render things. currently, we use an approach with numpy arrays and stuff, but it needs to be faster! ok, so been experimenting with PIL Image's `alpha_composite`, and things are looking pretty speedy! currently have been only testing with the loading images exactly on top of each other, and `alpha_composite` is limited by needing the same size image, but i think we can fix this? anyways, back to ivo! here are some test results by the way...

```
placeOver avg: 0.09378817319869995
pillow avg: 0.006969912052154541
```

ok, just did some work, and the new `placeOver()`, which uses `img.paste()` (not `alpha_composite`), now supports transparency and any position pasting! (still need to work on centering though). tests (n=100) have been very promising!

```
placeOver avg: 0.057012906074523924
neoPlaceOver avg: 0.0028543877601623535
```

`neoPlaceOver()` (temporary name for now), is roughly 20x faster than the original `placeOver()`, and does stuff in PIL Images instead of numpy arrays, which is ok for now, just requires a lot of renaming and rewriting stuff! instead of doing calculations in numpy, we do them in Pillow! generators will probably initially generate them as numpy arrays, then convery the arrays into images for future use. ok, back to IVO, finishing centering! afterwards, lets finish up with splitting `interface.py` into multiple files, and then implement this new system!