# 1/20/2025 - 74th Update!

ok, time to code, 1:15 PM here in NYC, the day right before midyears! (uh oh)

so, its been a bit, and been working on the little bits of the UI (its very tedious, change a number, run the code, change it again, repeat), but its definetly getting somewhere!

![yay](</updatelogs/images/132025/01202025 - 1.png>)

ok, been thinking of it again, so here's the better plan for rendering nodes:
- basic node gets rendered first (contains: node main rectangle, and side traingles, consistent x across all, and y depends)
- then node connections (the lines and curves for connections and stuff)
- then semi-transparent labels for the nodes get rendered

this is so that the labels for each input and output is still visible at the end, and also makes the coding side a bit prettier

ok, so `MAX_UNCAPPED_NODES_LIMIT` setting has now been removed, as it just didnt really make sense anymore!

ok, so node stuff is going well right now, a bit slow, but hoping to pick up the pace! however, later today, ill be forced to go outside to eat outside with family, so that means im either gonna have to speedrun studying or somehow multitask... uh oh

no way, theres an AP world history podcast? hmm... oh this is from like 2020

ok, so outputs arent rendering... idk about inputs... also, another future challenge is getting nodes to be "loaded in" without having inputs, basically making that `__init__()` code do something else, and make `update()` not run until it is safe to do so (no errors, meaning all necessary inputs and correct node types)

![waw](</updatelogs/images/132025/01202025 - 2.png>)

ok, fixed it! so turns out i did a bit of calculations wrong, and also fixed it being aligned to the wrong area, with the downside that there'll (is that a word?) always be that transparent bit on the top (currently red), but that shouldnt impact performance too too much...

ok, just increased the size of things, now its time to add the little node triangles and stuff! been thinking about the exact shape (rectangles and be calculated quickly, circles can be easily calculated but a bit tedious on the system, triangles are "what how do i program that"), but while typing that out i just realized we can have a traingle template in the resources, and just have it switch out colors to the ones we need, similar to how the tab icons were done!

unforutnately, however, i will be forced to go outside any second now, so i guess ill review for exams on that trip... anyways, here's the commit!

ok, so its now 11 PM, and i just speedran studying for midyears, at least for tommorow, so the plan is that my first exam should be ok, then i get 3-4 hours of studying for my second exam, and then i head home tommorow, speedrun studying for wednesday (shouldn't be too bad), then work on this! alright, back to updates on tape v2, the visual number node is now hooked up to the rendering workspace system, and now moves along with everything! nice! now its time to add the "fill-in" display stuff, and connections, and the triangle thing, woo, got some things to do!

also, every day that i spend in Java, i want the object stuff more and more, like the `public int example(String example){return 0;}`, since you can clearly define what comes in and out, along with the defining what types/classes/objects variables have to be, such as `private final Example example = new Example();`

anyways, rant over, good night, a bit worried about tommorow, and yeah, till next time! (hopefully tommorow!)