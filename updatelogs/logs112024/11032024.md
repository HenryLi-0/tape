# 11/3/2024 - 52nd Update!

guten tag! (no, i dont know german. that green bird from the language learning app has seen me done one lesson though)

ok, so yesterday, i moved a couple things and renamed a couple bits relating to the `State` class and the `Interface` class, and decided that `interface.py` would handle the user inputs and interacting stuff while `state.py` would just be about what state the program is in!

anyways, swapped out the `placeOver()` system over there in IVO, and here are the results!

```
original:
avg of 1095 frames: 0.03700961282808487 seconds
new:
avg of 1095 frames: 0.0294141542965963 seconds
```

that's a ~0.01 second difference in rendering, but that's a 20.5% decrease in rendering time! however, i just realized that i forgot a bunch of significant bits and bobs that also need some quick updates (the image modifying things, like apperances and stuff) right, one more thing, the new `placeOver()` can also center images now!

ok, something weird's happening with rotating and clipping stuff out, which only seems to happen when centering the image? idk, strange behavior, i'll look more into it tommorow! it feels weird not cramming everything on sunday evening, am i missing something?

anyways, good night! (wait a minute, let me create some UI concepts and show it tommorow, i thought of something fairly interesting earlier... what if we made the whole system a node graph based?)