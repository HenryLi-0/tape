# 1/8/2025 - 69th Update!

~~oh look its the funny number~~

ok, so it appears i have a suspiciously low amount of homework today, so thats a little weird. anyways, that means i can spend probably like one or two hours here! ok, lets jump straight in! (after doing FRC programming and java for quite a lot these past two weeks, im strangely thinking in java?)

so, one thing i thought of in the shower, adding a priority system for caclulating ivos (like, which ones should be on top) would be pretty nice! (unforunately, not gonna do that just yet...)

ok, been doing some thinking, so `visualnode.py` will have a `VisualNode` that is essentially a visual object that wraps multiple visual object to basically treat them as one... this might be pretty difficult, so lets get to it!

so basic rules:
- we create textboxes if its a string, boolean, or number, otherwise *should* never create textboxes
  - if values are inputted instead, textboxes should be hidden, and the inputted value should be used instead (textbox should not be destroyed, just hidden for easy reopening)
- if its not a textbox, it displays the best way it can express the value (ex. a picture for a picture, graph for a graph, etc.) (might be a v2.1 feature for more advanced stuff)
- type at the very top
- inputs below
- outputs below

ok, just realized that it's really hard to do this without a functioning rendering workspace, so that should probably be done first... ok, zoom is working! you can also now move around with arrow keys (its also smooth for satisfying)

getting a bit tired, but defintely feeling more familiar with parts of the code again! ok, see you (hopefully) tommorow, good night! (also, `visualnode.py` has been moved to `workspace.py`!)