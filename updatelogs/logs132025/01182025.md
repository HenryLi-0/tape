# 1/18/2025 - 72nd Update!

ok, thats not good... anyways, the last couple days have been very tiring as every teacher decided to give midyears review homework... so i did do a bit of work on nodes these days, but not enough work to exactly "complete" the tasks, so that's what ill spend some of tomorrow doing! (this update also contains the work of those little bits of work from previous days!)

ok, so quick updates on what was done, just a bunch of `@Inputs` and `@Outputs`, for `node_pathing.py` and the new `node_render.py`! still need to work on `node_operations.py` logic... ok, im back, and unfortunately, it seems that i cant think right now about it, so lets just work on UI now!

so, when considering how to "format" these nodes visually, im not really sure a in and out for rows and things would work, since theres gonna be many rows. alternatively, we can have triangles for ins and outs and a text box on the side of it that disappears when connected, but it may be hard to manage. a third idea is to add a massive IO area of each node that manages ins and outs, which would look pretty atrocious, if theres just a giant area of ins and outs, since the nodes would be HUGE

now that im thinking about it, the second idea might work, with the downside of being a bit confusing to first time users, but that's ok! just realized, we need to find a way to get node data to display on the node, should be pretty fun to add

ok, first thing tommorow, add `@Display` decorators for things, and then start UI stuff!

ok, good night!